
// We load a modularised version of the original customisations from Bootstrap
// Package. These customisations include Bootstrap overrides and all the
// styling rules necessary for the defined content elements, which are either
// native to Bootstrap (e.g. accordion) or are special components provided only
// by Bootstrap Package, thus not native to Bootstrap (e.g. lists of file links).
import '../Scss/BootstrapPackageCustomisations/bootstrap-package-customisations.scss'

// Leave a mark.
console.log('You are using the NetBrothers TYPO3 Base Template. Thanks! ❤️');
